// Definition function
function rectangleArea1(width: number, length: number): number {
    return width * length;
};

// console.log(rectangleArea1(3, 5));

// Arrow functions
const rectangleArea2 = (width: number, length: number): number => {
    return width * length
}

console.log(rectangleArea2(3, 5));
