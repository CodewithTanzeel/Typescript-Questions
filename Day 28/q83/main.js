function convertingText(text) {
    var uppercase = text.toUpperCase();
    var lowercase = text.toLowerCase();
    console.log('Uppercase:' + " " + uppercase);
    console.log('Lowercase:' + " " + lowercase);
}
convertingText("Hi User");
