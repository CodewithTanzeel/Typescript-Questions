function convertingText(text: string) {
    let uppercase = text.toUpperCase();
    let lowercase = text.toLowerCase();
    console.log('Uppercase:'+ " " + uppercase);
    console.log('Lowercase:'+ " " + lowercase)
}

convertingText("Hi User")