var dayjs = require('dayjs'); // Import dayjs
var currentDate = dayjs(); // Get the current date
var formattedDate = currentDate.format('DD-MM-YYYY'); // Format the date
console.log(formattedDate); // Output the formatted date
