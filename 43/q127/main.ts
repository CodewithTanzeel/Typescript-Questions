// Function Expression
function sum1(num1: number, num2: number): number {
    return num1 + num2;
}

console.log(sum1(2,2)); // by function expression

// Arrow Function
const sum2 = (num1: number, num2: number): number => { 
    return num1 + num2;
}

console.log(sum2(2,2)); // by arrow function 