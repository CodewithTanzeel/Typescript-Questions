// Using For Loop to print numbers from 1 to 10
for (let i = 1;     // initializer: sets i to 1
    i <= 10;       // condition: loop continues while i is less than or equal to 10
    i++) {         // increment: increases i by 1 after each iteration

   // Log the current value of i in each iteration
   console.log(i);
}
