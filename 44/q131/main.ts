/**
 * Importing the Shop class from the 'class' module.
 */
import { Shop } from "./class";

/**
 * Creating a new instance of the Shop class with product name "Wood" and price 20.
 */
const slot = new Shop("Wood", 20);

/**
 * Displaying the product name and calculated price using the displayNameAndPrice method.
 */
slot.displayNameAndPrice();
