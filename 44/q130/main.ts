// Import the 'UpperCase' function from the './case' module
import { UpperCase } from "./case";

// Call the 'UpperCase' function with the argument 'heading' and log the result
console.log(UpperCase("heading")); 
// Expected output: 'HEADING' if the function converts the input string to uppercase
